name: tree_processing_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas
  - biopython
  - tqdm
  - ete3
  - requests
  - pip
  - pip:
      - rdkit
      - wikidata-dl
      - Pillow
      - networkx
      - psycopg2
      - formula_validation
