name: tree_processing_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3
  - pandas
  - biopython
  - ete3
  - requests
  - pip
  - pip:
      - rdkit
      - wikidata-dl
      - Pillow
